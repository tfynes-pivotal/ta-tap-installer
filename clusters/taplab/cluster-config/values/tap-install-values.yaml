#@ load("@ytt:data", "data")
---
tap_install:
  package_repository:
    #@ if data.values.platformRegistryLocation == 'customPlatformRegistrySelection':
    oci_repository: #@ data.values.customPlatformRegistryUrl + '/tap-packages'
    #@ else: 
    oci_repository: tanzu.packages.broadcom.com/tanzu-application-platform/tap-packages
    #@ end
  version:
    package_repo_bundle_tag: "1.12.1"
    package_version: "1.12.1"
